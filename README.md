# gator blog aggregator

to install run:
go install

set up config file:
Set a config file named ".gatorconfig.json" with:

```json
{
  "db_url":<connection_string>,
  "current_user_name":<username>
}
```
